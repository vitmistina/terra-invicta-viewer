{
  "name": "terra-invicta-viewer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "serve": "uv run python -m http.server 8080"
  },
  "engines": {
    "node": ">=20"
  }
}
